# ==========================================================================
# Auction Finalization Workflow — POST /cre/finalize
# ==========================================================================
# Input: auctionId. Loads sealed bids from store; computes clearing price;
# calls SilentBidCCA.forwardBidsToCCA(...) (admin). See plan_execution.md.
# ==========================================================================

staging-settings:
  user-workflow:
    workflow-name: "silentbid-finalize-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "silentbid-finalize-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../../secrets.yaml"
